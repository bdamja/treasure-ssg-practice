# Forceload the necessary chunks, so that the
# player can play on 2 render distance. We need
# to load the chunks with armor stands, and the
# chunks with structure blocks.
# https://minecraft.fandom.com/wiki/Commands/forceload

# example: main lobby
forceload add 0 0
